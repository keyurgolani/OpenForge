category: http
description: "Make raw HTTP API requests"
tools:
  - name: http.get
    description: "Make an HTTP GET request"
    risk_category: moderate
    confirm_by_default: false
  - name: http.post
    description: "Make an HTTP POST request"
    risk_category: moderate
    confirm_by_default: true
